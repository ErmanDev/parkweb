# Local dev: leave empty (Nuxt serves /api on the same host).

# Vercel (recommended): leave NUXT_PUBLIC_API_BASE unset.
# vercel.json proxies /api/* → https://parkweb.onrender.com/api/*

# Only set this if you are NOT using vercel.json rewrites:
# NUXT_PUBLIC_API_BASE=https://parkweb.onrender.com
