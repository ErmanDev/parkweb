# Local dev: leave empty (Nuxt serves /api on the same host).

# Vercel: leave NUXT_PUBLIC_API_BASE unset — app uses https://parkweb.onrender.com.
# If set, must be the current Render URL:
# NUXT_PUBLIC_API_BASE=https://parkweb.onrender.com
